# Core audio processing
librosa>=0.10.0
numpy>=1.23.0
scipy>=1.10.0
soundfile>=0.12.1

# Visualization (spectrogram images)
matplotlib>=3.7.0

# ML utilities (cosine similarity, etc.)
scikit-learn>=1.3.0

# ---------------------------------------------------------------
# OPTIONAL: only needed for the higher-accuracy speaker verification
# mode (SpeechBrain's pretrained ECAPA-TDNN model). The project runs
# fine without it — speaker_verifier.py will automatically fall back
# to a lightweight MFCC-based comparison if this is not installed.
#
# Uncomment the two lines below if you want the more accurate mode:
# speechbrain>=1.0.0
# torch>=2.0.0
# ---------------------------------------------------------------
